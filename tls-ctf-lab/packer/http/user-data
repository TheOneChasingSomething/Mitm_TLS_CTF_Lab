#cloud-config
users:
  - name: lab
    sudo: ALL=(ALL) NOPASSWD:ALL
    plain_text_passwd: 'labtp'
    lock_passwd: false
    shell: /bin/bash

ssh_pwauth: true

chpasswd:
  list: |
    lab:labtp
  expire: false

runcmd:
  - ssh-keygen -A
  - systemctl restart ssh