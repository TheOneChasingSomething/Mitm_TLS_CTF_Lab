---
# Mode All-In-One — Reproduction locale autonome du TP sur la VM.
# Déploie le portail, les cibles, la victime et l'attaquant sur vm_prof.
#
#   ansible-playbook standalone.yml -i inventory.ini

- name: Déploiement Standalone sur VM Prof
  hosts: teacher
  become: true
  roles:
    - common
    - portal
    - targets